akka.test {
  single-expect-default = 60s
  default-timeout = 60s
}

twitter {

  consumer {
    key = "my-consumer-key" 
    secret = "my-consumer-secret"
  }

  access {
    key = "my-access-key"
    secret = "my-access-secret"
  }
}
